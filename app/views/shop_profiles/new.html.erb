<% if user_signed_in? && current_user.is_admin? %>
    <h1>New Shop Profile</h1>

    <%= render 'form', shop_profile: @shop_profile %>

    <%= link_to 'Back', shop_profiles_path %>
<% else %>
    <% controller.redirect_to root_path%>
<% end %>
